let numero = 5

numero = numero + animale // ele reclama com isso porque isso é realmente incorreto no typescript

console.log(numero) // isso o javacript deixa, o typescript nao permite (tem que ser tipado)